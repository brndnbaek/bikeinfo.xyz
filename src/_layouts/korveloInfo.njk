<!DOCTYPE html>
<html lang="en">

<head>
	{% include "head.njk" %} {% if site.env %}
	<!-- capture the CSS content as a Liquid variable -->
	{% set css %}{% include "app.compiled.css" %}{% endset %}
	<style>
		{{css | safe}}
	</style>
	{% else %}
	<link rel="stylesheet" href="/static/app.compiled.css" />
	{% endif %}

</head>

<body class="w-screen h-screen p-0 antialiased text-gray-200">
	{% include "overlayMenu.njk" %}
	<!-- vertical stripes  -->
	<div class="relative">
		<div class="neomorph absolute top-0 bg-pink-400 z-0" style="right:14%; width:2%; height:2500px;"></div>
		<div class="absolute h-screen top-0 bg-gray-300 z-0" style="right:12%; width:2%; height:2500px;"></div>
		<h1 class="absolute text-gray-300 text-2xl md:text-4xl font-serif text-center z-20"
			style="right:6%; writing-mode: vertical-rl; text-orientation:sideways;height:600px;">Team Korvelo Infomation
		</h1>
	</div>
	<!-- top title -->
	<nav class="pl-5 md:pl-40">
		<a href="/" class="underline bg-pink-500 text-pink-500">
			<h1 class="font-extrabold text-gray-200 font-sans text-4xl md:text-6xl text-left uppercase">Team Korvelo
			</h1>
		</a>
	</nav>

	<div class="relative imageOverlay flex flex-col content-center mt-10 w-4/5 ml-5 z-0 pb-24" style="">
		<style>
			.imageOverlay::after {
				content: "";
				position: absolute;
				top: 0;
				right: 0;
				width:90%;
				height:90%;
				background-image: radial-gradient(ellipse closest-side, rgba(0,0,0,0.6), #090909), url({{heroImage}});
				background-size: cover;
				background-repeat: no-repeat;
				opacity: 0.9;
			}
		</style>
		<div class="relative w-5/6  ">
			<div class="z-20 w-full relative">
				{# <h1 class="text-gray-200 font-korTitle text-3xl mb-10">{{title}}</h1> #}
				<p class="text-gray-400 font-sans text-base md:text-xl w-5/6 pt-40 pb-40">Team Korvelo 정보 페이지</p>
				{# <p class="text-gray-400 font-sans text-base md:text-xl w-5/6 mb-4 line">{{postlist}}</p> #}
				
			</div>
		</div>
	</div>

	<div class="mx-auto w-4/5 flex flex-row flex-wrap">
		<div class="w-1/5 text-gray-300 text-base">이메일 주소를 ***으로 알려주시면 캘린더 작성/참여 권한을 드립니다. 코르벨로 일정을 추가하거나 참석 여부를 멤버들과 공유할 수 있습니다.</div>
		<div class="flex-1">
			<iframe src="https://calendar.google.com/calendar/embed?height=600&amp;wkst=1&amp;bgcolor=%23ffffff&amp;ctz=America%2FNew_York&amp;src=aHMycGg3NWptZGZhYW9hcTJjMXBnNG10YmdAZ3JvdXAuY2FsZW5kYXIuZ29vZ2xlLmNvbQ&amp;color=%23616161" style="border:solid 1px #777" width="800" height="600" frameborder="0" scrolling="no"></iframe>
		</div>
	</div>
	<div class="w-5/6 mx-auto text-red-200 flex flex-row flex-wrap">
		{% set listing = collections[postlist] %}
		{% set postlistCounter = collections.posts | length %}
			
			{% for item in	collections.korvelo %}
			{%if item.data.tags == "youtube" %}
			<div class="bg-gray-900 rounded-md m-6">

				<div class="relative"><a href="{{ item.url | url }}">{{ item.data.title }}</a></div>
				<div class="relative">
				<iframe width="560" height="315" src="{{item.data.featuredYoutube | url}}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
				</div>
			</div>
			{%else%}
		<div class="bg-gray-900 rounded-md m-6">
			<div class="relative"><a href="{{ item.url | url }}">{{ item.data.title }}</a></div>
			<div class="relative">
				<img class="" src="{{item.data.heroImage}}" alt="" srcset="">
			</div>
		</div>
		{%endif%}
			{% endfor %}
		

		
	</div>
	
</body>
<script src="/static/app.bundled.js"></script>
<script src="https://cdn.jsdelivr.net/npm/lax.js"></script>
<script>
	function navToggle() {
		var btn = document.getElementById("courseBtn");
		var overlay = document.getElementById("courseOverlay");
		var xbtn = document.getElementById("closeBtn");

		console.log("abcd");
		// btn.classList.toggle("open");
		overlay.classList.toggle("hidden");
		// xbtn.classList.toggle("block");
	}

	window.onload = function () {
		lax.setup() // init

		const updateLax = () => {
			lax.update(window.scrollY)
			window.requestAnimationFrame(updateLax)
		}

		window.requestAnimationFrame(updateLax)
	}
</script>

</html>
